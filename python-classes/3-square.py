#!/usr/bin//python3
"""create a square"""
class square:
    """instance att is size"""
    def _init_(self,size=0)
    if not instance(size, init)
    raise typeError("size must an integer")
elif size<0:
    raise TypeError("size must be >=0")
self._size=size
def area(self):
square.area=self._size**2
return square_area
